meta:
  environment: python
  toolchain:
    name: pip
    version: 3.13
build:
  requirementsPath: requirements.txt
run:
  scriptName: python -m app
  command: python -m app
  container:
    cmd: python -m app